type Query {
    games: [Game]
    gamesByPlayer(playerId: ID!): [Game]
}

type Game {
    id: ID!
    numOfPlayers: Int!
    status: String!
    players: [Player]!
    arena: Arena!
}

type Player @key(fields: "id") @extends {
    id: ID! @external
}

type Arena {
    id: ID!
}