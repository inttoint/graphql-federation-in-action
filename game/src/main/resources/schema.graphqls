type Query {
    games: [Game]
    gameById(id: ID!): Game
}

type Game {
    id: ID!
    numOfPlayers: Int!
    status: String!
    players: [Player]!
    arena: Arena!
}

type Player {
    id: ID!
}

type Arena {
    id: ID!
}